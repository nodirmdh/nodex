{
  "name": "nodex",
  "private": true,
  "version": "0.1.0",
  "type": "commonjs",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm -w apps/api run dev",
    "dev:admin": "npm -w apps/admin-web run dev",
    "test": "npm -w apps/api test",
    "test:integration": "RUN_INTEGRATION=1 npm -w apps/api test",
    "build": "npm -w apps/api run build && npm -w apps/admin-web run build",
    "typecheck": "npm -w apps/api run typecheck && npm -w apps/admin-web run typecheck"
  }
}
